{
  "name": "smart-grade-predictor-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "vercel-build": "yarn --cwd apps/web install && yarn --cwd apps/web build"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  },
  "dependencies": {
    "apollo-server": "^3.13.0"
  }
}
